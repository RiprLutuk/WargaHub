# Read-only sample responses are opt-in and never apply to authentication or writes.
VITE_ENABLE_DEMO_FALLBACK=false

# Shows seeded credential shortcuts on the login screen; login still calls the API.
VITE_SHOW_DEMO_CREDENTIALS=false
